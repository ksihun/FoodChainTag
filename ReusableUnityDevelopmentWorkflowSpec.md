# 재사용 가능한 Unity 개발·검증 워크플로 명세

상태: **템플릿**  
용도: 새 Unity 프로젝트에서 AI 협업 개발의 작업 방식, Goal 명세, 검증 기준을 통일한다.

## 1. 목적

이 문서는 기능을 바로 구현하기 전에 **무엇을 만들고, 어디까지 만들며, 어떻게 완료를 판정할지** 먼저 고정하기 위한 템플릿이다.

모든 작업은 다음 원칙을 따른다.

- 작은 단위의 Goal을 먼저 문서화한 뒤 구현한다.
- 코드·씬·프리팹·ScriptableObject 데이터 변경을 한 작업 단위로 추적한다.
- 수정 뒤에는 컴파일과 Console Error를 반드시 확인한다.
- 기능 규모에 맞춰 EditMode, PlayMode, 실제 빌드 검증을 추가한다.
- 완료하지 못한 항목과 범위 밖 항목을 결과 문서에 명확히 남긴다.

## 2. 프로젝트에 처음 적용하는 방법

새 프로젝트의 루트에 다음 파일을 둔다.

| 파일 | 역할 |
| --- | --- |
| `AGENTS.md` | AI가 Unity를 수정·검증할 때 지킬 공통 작업 규칙 |
| `Docs/NextMilestone.md` | 여러 Goal의 우선순위와 진행 상태를 관리하는 로드맵 |
| `Docs/GoalXX_<Name>Spec.md` | 큰 기능 하나의 상세 설계·완료 기준·결과 기록 |
| `Docs/TestPlan.md` | 프로젝트 전체의 대표 플레이 흐름 및 출시 QA 체크리스트 |

`AGENTS.md`는 Hera Agent 사용 규칙을 포함할 수 있다. 새 프로젝트에서 해당 도구를 쓴다면 연결 상태와 제공 도구를 먼저 확인하고, 사용하지 않는다면 Hera 관련 문단을 제거하거나 다른 Unity 검증 도구로 치환한다.

## 3. 기본 작업 루프

모든 코드·씬·Inspector 작업은 아래 순서를 기본으로 한다.

1. **Goal 확인**: 이번 작업의 목적, 범위, 완료 조건을 한 문장으로 확인한다.
2. **현재 상태 관찰**: 바꿀 스크립트, 데이터, 씬, 프리팹, 기존 테스트만 필요한 범위에서 읽는다.
3. **구현**: Goal 범위 안에서 코드·에셋·씬을 수정한다.
4. **컴파일 확인**: Unity 컴파일 오류가 없는지 확인한다.
5. **Console 확인**: 새 Error가 없는지 확인한다.
6. **변경 대상 재확인**: 참조, Inspector 값, 데이터 ID, 씬 연결을 다시 읽는다.
7. **기능 검증**: 아래 검증 단계에 따라 EditMode/PlayMode/빌드를 수행한다.
8. **결과 기록**: 완료 여부, 검증 결과, 남은 위험 요소를 Goal 문서에 남긴다.

문제가 생기면 원인을 확인하고 1~2회 수정·재검증한다. 여전히 해결되지 않으면 추측으로 범위를 넓히지 말고, 현재 상태와 막힌 조건을 기록한다.

## 4. 검증 단계

### 4-1. 모든 변경에 필요한 최소 검증

- Unity 컴파일 오류 0건
- Unity Console Error 0건
- 수정한 파일·씬·에셋의 참조가 의도대로 연결됨

### 4-2. 규칙·계산·데이터 변경

다음 중 하나를 바꾸면 관련 EditMode 테스트를 추가 또는 실행한다.

- 피해·보상·확률·쿨다운 등 계산식
- ScriptableObject의 ID, 데이터 구조, 데이터베이스 등록
- 퀘스트 조건, 저장 데이터, 해금 조건
- 패턴 판정, AI 행동, 인벤토리 규칙

테스트는 정상 사례뿐 아니라 빈 데이터, 중복 ID, 최대/최소 수치, 잘못된 참조처럼 실패하기 쉬운 경계 조건을 포함한다.

### 4-3. 씬·UI·입력·연출 변경

다음 중 하나를 바꾸면 대표 PlayMode 시나리오를 확인한다.

- 씬 전환, 스폰, 복귀 위치
- 버튼, 드래그, 스크롤, ESC 등 입력 처리
- UI 레이아웃, 해상도 대응, 오버레이 순서
- Animator, VFX, SFX, 카메라 연출
- NPC 대화, 상점, 전투 진입·종료

확인 항목은 “오브젝트가 존재한다”가 아니라 **플레이어가 실제로 입력했을 때 의도한 결과가 나온다**로 작성한다.

### 4-4. 마일스톤·출시 전 검증

기능을 묶어 배포하거나 발표할 빌드를 만들기 전에는 다음을 수행한다.

- 전체 EditMode 테스트
- 콘텐츠 검증 도구 또는 ID·참조 검사
- 핵심 플레이 루프 1회 완주
- 새 게임, 저장/로드, 실패/재시도, 재입장 등 회귀 흐름
- 지원 해상도에서 UI 잘림·겹침·입력 문제 확인
- 실제 빌드에서 Console Error 0건

## 5. Goal 작성 템플릿

```md
# Goal N — 기능 이름

상태: **계획**
작성일: YYYY-MM-DD

## 목적
- 플레이어 또는 개발 과정에서 해결할 문제

## 현재 상태 / 문제 진단
- 현재 동작과 재현 조건
- 관련 코드·데이터·씬의 현 상태

## 범위
### 포함
- 이번 Goal에서 구현·수정할 항목

### 제외
- 이번 작업에서 의도적으로 다루지 않을 항목

## 설계
### 데이터 규칙
- ScriptableObject, ID, 수치, 참조 규칙

### 구현 구조
- 수정 대상 스크립트, 씬, 프리팹과 책임 분리

### UI / 연출 규칙
- 필요할 경우 화면, 입력, 애니메이션, 사운드 기준

## 작업 순서
1. 데이터·에셋 준비
2. 코드·씬 연결
3. UI·연출 적용
4. 검증

## 완료 조건
- 플레이어 관점의 결과
- 회귀하면 안 되는 기존 동작
- 필요한 자동/수동 검증 조건

## 검증 계획
- EditMode:
- PlayMode:
- 실제 빌드:

## 구현 결과
- 실제 변경 내용
- 검증 결과
- 남은 작업 및 후속 Goal
```

## 6. `NextMilestone.md` 작성 템플릿

```md
# 다음 마일스톤 계획

## 현재 기준점
- 현재 빌드 상태:
- 자동 테스트:
- 알려진 오류:
- 이번 마일스톤의 제한 사항:

## Goal N: 기능 이름

상태: 계획

목표:
- 이번 Goal이 끝났을 때 가능한 플레이 경험

### 고정 범위
1. 반드시 구현할 항목
2. 반드시 검증할 항목

### 완료 판단
- 완료로 볼 수 있는 플레이어 관점의 조건

### 제외 범위
- 다음 Goal로 미루는 항목

### 위험 요소 / 선행 조건
- 필요한 아트·사운드·데이터·다른 Goal
```

## 7. 작성 원칙

- Goal 하나는 검증 가능한 결과 하나를 만든다. “전투 전체 개선”처럼 넓은 제목 대신 “특수 공격 모션 연결”처럼 관찰 가능한 결과로 쪼갠다.
- 수치, ID, 파일명, 씬 이름처럼 변경에 취약한 사실은 표나 목록으로 명시한다.
- “잘 동작한다” 대신 입력, 조건, 기대 결과를 적는다. 예: “보스 처치 후 허브의 원래 위치로 복귀한다.”
- 자동 테스트가 어려운 시각·감각 요소는 PlayMode 체크리스트로 남긴다.
- 완료 문서에는 계획과 실제 결과를 섞지 않는다. 실제 적용 후에는 `구현 결과`에 별도로 기록한다.
- 새로운 기능을 시작하기 전, 이전 Goal의 완료 조건과 회귀 검증이 충족됐는지 먼저 확인한다.

## 8. 권장 최소 체크리스트

### 일반 기능

- [ ] 범위와 제외 범위가 문서화됨
- [ ] 컴파일 오류 없음
- [ ] Console Error 없음
- [ ] 수정 대상 참조 확인
- [ ] 관련 자동 테스트 또는 수동 테스트 완료
- [ ] 결과와 남은 작업 기록

### 씬 전환·진행도 기능

- [ ] 진입 위치 확인
- [ ] 복귀 위치 확인
- [ ] 저장/로드 후 상태 확인
- [ ] 퀘스트·해금·보상 연결 확인
- [ ] 새 게임과 이미 진행된 저장 모두 확인

### 출시 전

- [ ] 전체 EditMode 테스트 통과
- [ ] 대표 PlayMode 루프 완주
- [ ] 실제 빌드 실행
- [ ] 입력·해상도·오디오·저장 확인
- [ ] Console Error 0건

## 9. 결과 기록 예시

```md
## 구현 결과 (YYYY-MM-DD)

- `EnemyDataSO`에 약점 데이터와 보상 값을 추가했다.
- 전투 초기화 시 해당 데이터를 읽도록 연결했다.
- EditMode: `BattleFormulaTests` 및 `ContentDataValidatorTests` 통과.
- PlayMode: 보스전 진입 → 승리 → 필드 복귀를 확인했다.
- Unity Console Error 0건 확인.

남은 작업:
- 보스 사망 연출은 다음 Goal에서 연결한다.
```
